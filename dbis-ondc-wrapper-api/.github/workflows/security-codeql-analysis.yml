name: Security CodeQL Analysis

on:
  push:
    branches: [ main ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ main ]
  schedule:
    - cron: '42 2 * * 6'

jobs:
  release:
    uses: dnb-main/devx-github-org-workflows/.github/workflows/security-codeql.yml@main
    secrets: inherit